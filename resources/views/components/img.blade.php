<img
    height="{{ $height }}"
    width="{{ $width }}"
    src="{{ $src }}"
    alt="{{ $alt }}"
    {{ $attributes->merge(['style' => 'display: block; outline: none; border: none; text-decoration: none;']) }}
/>
